# CME295 Transformers & LLMs — Lecture 9 한국어 교재
## 섹션 6. 마무리 — 진행 중인 연구·미래·자료

> 원문 출처: Stanford CME295, Autumn 2025, Lecture 9, 타임스탬프 (1:23:53)~(1:51:14, 끝)

---

## 1. 강의 개요

- 모달리티 간 상호 영향(디퓨전·트랜스포머·입력·트릭의 교차 차용)과, 트랜스포머 연구가 여전히 활발하다는 점을 본다.
- 진행 중인 연구 — 최적화기(Muon)·정규화·활성함수·데이터(모델 붕괴)·하드웨어(아날로그) — 와 미래 활용·과제를 정리한다.
- 최신 동향을 따라가는 자료와 강사들의 맺음말로 마무리한다.

---

## 2. 학습 목표

1. 모달리티 간 차용(디퓨전·ViT·DeepSeek-OCR·2D RoPE)의 예를 설명할 수 있다.
2. 트랜스포머의 여러 설계가 여전히 연구 중임(최적화기·정규화·데이터·하드웨어)을 안다.
3. 미래 활용·과제와, 최신 동향을 따라가는 자료를 안다.

---

## 3. 본문

### 블록 1 — 모달리티 간 상호 영향

**영문 원문**
> (1:24:24) diffusion (...) born in the field of images but (...) taken for text (...) (1:24:57) replacing convolutions with transformers (...) better results (...) (1:25:58) deepsek OCR (...) learn some function that (...) text tokens based on (...) very few vision tokens (...) (1:27:35) rope (...) reformulating it in 2D.

**한국어 번역**
이미지와 텍스트가 서로 영향을 주고받습니다.
- **아키텍처**: 디퓨전(이미지 출신)이 텍스트로 와 속도를 높였고(섹션 5), 반대로 이미지 디퓨전이 합성곱(CNN) 대신 트랜스포머를 써 더 좋아졌습니다(디퓨전 트랜스포머).
- **입력**: DeepSeek-OCR — 이름과 달리 OCR 성능 자랑이 아니라, '아주 적은 비전 토큰'으로 텍스트 토큰을 표현하는 함수를 학습할 수 있음을 보였습니다. 이미지 패치 토큰의 표현력이 강하다는 것 — "토크나이저가 최선이 아니다"라는 논거(이모지 등은 텍스트로는 많은 토큰이 필요하지만 패치는 의미를 바로 담음)와 연결됩니다.
- **트릭**: RoPE(텍스트의 상대 위치)를 이미지·멀티모달에서 2D로 재구성해 적용 — 2D 격자에서 위치를 부여해 텍스트·이미지 토큰의 상대 위치 계산이 여전히 의미 있게 만듭니다.

**용어 설명**
- **디퓨전 트랜스포머**: 이미지 디퓨전에 트랜스포머를 쓴 것.
- **DeepSeek-OCR**: 적은 비전 토큰으로 텍스트를 표현하는 표현력을 보인 연구.
- **2D RoPE**: RoPE를 2차원으로 확장해 이미지·멀티모달에 적용.

---

### 블록 2 — 진행 중인 연구: 설계는 아직 확정 아님

**영문 원문**
> (1:28:38) the ongoing research for transformers is very much alive (...) the optimizer (...) atom (Adam) (...) Kim(i) K2 (...) muon (...) muon clip (...) (1:30:15) normalization (...) layer norm (...) RMS norm (...) (1:31:18) activation functions (...) ReLU (...) gausian error linear (units, GELU) (...) (1:31:51) number of layers (...) still up for debates.

**한국어 번역**
트랜스포머의 세부 설계는 여전히 연구 중이며, 각 결정이 계속 갱신됩니다.
- **최적화기(optimizer)**: 오래 표준이던 Adam이 새 논문으로 도전받습니다 — Kimi K2 논문이 Muon(과 최신판 MuonClip)이라는 새 최적화기를 제시, 차기 표준 후보로 거론됩니다.
- **정규화**: 위치(post-norm→pre-norm)뿐 아니라 종류도 바뀜 — 원논문의 layer norm 대신 요즘은 파라미터가 적은 RMSNorm 등.
- **어텐션 변형**: GQA 등 고정 설계가 아니라 논문마다 다르고, 층마다 다른 어텐션을 쓰기도.
- **활성함수**: 전통적 ReLU에서 GELU(가우시안 오차 선형 유닛) 같은 'ReLU 유사' 함수로 이동, 여전히 새 함수가 나옴.
- 그 외 MoE 여부·층 수·FFN 크기 등 하이퍼파라미터도 논쟁 중. 즉 아키텍처 자체가 최선인지도 열린 문제.

**용어 설명**
- **Adam → Muon/MuonClip**: 표준 최적화기에 대한 새 도전(Kimi K2).
- **RMSNorm / GELU**: 최근 자주 쓰는 정규화 / 활성함수.
- **설계 미확정**: 최적화기·정규화·활성함수·구조 모두 진행 중인 연구.

---

### 블록 3 — 데이터와 하드웨어

**영문 원문**
> (1:32:52) the data part (...) the first results are 80% LLM generated (...) (1:33:25) data curation (...) mid training (...) (1:34:29) model collapse (...) LLM generated text is (...) less (meaningful). (...) (1:36:32) smaller and smaller LLMs (...) SLM (...) (1:37:05) the hardware part (...) GPUs (...) great at (...) matrix multiply (...) (1:38:39) computations as a side effect of (...) analog signals (...) kir(ch)hoff (...) law.

**한국어 번역**
**데이터**: 초기 LLM은 인터넷을 긁으면 사람이 쓴 고품질 데이터를 얻을 수 있었습니다. 지금은 검색 결과의 80%가 LLM 생성일 정도로 바뀌었습니다. 그래서 데이터 큐레이션이 중요해졌고(단순 스크래핑→관심 데이터 선별), 사전학습-중간학습(mid-training, 더 고품질)-파인튜닝으로 단계가 늘었습니다. LLM 생성 데이터로 학습하면 모델 붕괴(model collapse) — 분포가 변해 학습 의미가 줄어드는 현상 — 가 보고됐고, 데이터 작업의 필요성을 강조합니다.

**비용·소형화**: 모든 쓸 일을 푸는 단계가 오면, 다음은 비용 효율의 Pareto 프런티어로 — 점점 작은 모델(SLM, Small Language Model)이 부상합니다. LLM 제공자가 최고 요금제에서도 손해 본다는 말은, 추론 시 연산을 더 영리하게 써야 함을 보여줍니다.

**하드웨어**: GPU는 행렬 곱에 강하지만, 트랜스포머엔 그것만이 아닌 특수 연산(QKᵀ 등 비싼 부분)이 있습니다(FlashAttention이 메모리 이동을 줄이려 재계산까지 한 이유). 그래서 더 맞춤한 하드웨어 연구가 나옵니다 — 한 논문은 연산을 아날로그 신호의 부수효과로 하드웨어에 새깁니다(키르히호프 법칙 같은 물리 성질로 입력 펄스를 더해 결과를 읽음). 시뮬레이션 결과 지연·에너지 모두 크게 개선됐습니다(연산을 직접 안 하고 부수효과로 얻으므로).

**용어 설명**
- **데이터 큐레이션 / 중간학습(mid-training)**: 데이터를 선별 / 사전학습 후 고품질 단계.
- **모델 붕괴(model collapse)**: LLM 생성 데이터로 학습 시 분포가 망가지는 현상.
- **SLM(Small Language Model)**: 비용 효율을 노린 소형 모델.
- **아날로그 하드웨어**: 연산을 물리 신호의 부수효과로 수행하는 새 하드웨어 연구.

---

### 블록 4 — 미래 활용과 과제

**영문 원문**
> (1:40:15) how to use them (...) coding (...) general assistant (...) creativity (...) brainstorming. (...) (1:43:25) democratized (...) natural language no need to code. (...) (1:44:26) chat GPT's atlas (...) security (...) exfiltrate. (...) (1:46:31) continuously (learn) (...) hallucinations (...) core design (choice).

**한국어 번역**
**오늘의 활용**: 코딩 보조(자연어→코드, 에이전트 모드), 범용 비서(사실 조회·웹 탐색), 창의 작업(마케팅 등 백지에서 초안), 학습 보조(개념 브레인스토밍 — 강의 중 학생들이 ChatGPT로 개념을 찾아보는 것은 학습에 좋음).

**다가오는 것**: 에이전트 워크플로의 대중화(코딩 없이 자연어로 유용한 것을 만듦), AI의 웹 브라우징(예: ChatGPT Atlas, 2025년 10월 — 단 보안 과제: 악성 프롬프트 주입·데이터 유출. 미래엔 'AI 브라우징 안전 인증서' 같은 게 나올 수도), OS 수준 보조 등.

**남은 과제**: 에이전트는 단계가 늘수록 실패 확률↑(안정화가 관건). AI 고객센터가 진짜 유용한가는 좋은 시험대(사람은 시스템 프롬프트에 없는 것도 분별하는 등 가치 차원이 많음). 현재 아키텍처의 근본 과제 — ① 학습 후 가중치가 안 변함(RAG·도구로 우회하지만, 지속 학습은 열린 문제), ② '환각'(LLM은 사실 대응이 아니라 다음 토큰 예측으로 학습됐으니 환각은 어느 정도 설계상 본질), ③ 개인화·해석가능성·안전 등.

**용어 설명**
- **대중화(democratization)**: 코딩 없이 자연어로 누구나 도구를 만듦.
- **지속 학습(continual learning)**: 배포 후에도 계속 배우는 것. 열린 과제.
- **환각(hallucination)**: 사실과 다른 생성. 다음 토큰 예측이라는 설계상 본질적 측면.

---

### 블록 5 — 최신 동향 따라가기와 맺음말

**영문 원문**
> (1:47:36) archive (arXiv) (...) new rips (NeurIPS) (...) associated code bases (...) paper with code (...) hugging face(') s (...) trending papers (...) (1:48:34) Twitter or X (...) YouTube (...) Yanik Kil(cher) (...) Andr(ej) Karpath(y) (...) company blogs (...) study guide.

**한국어 번역**
**최신 동향 따라가기**:
- 논문: arXiv(최신 논문), NeurIPS 같은 학회. 논문에 딸린 코드베이스도 함께 보면 통찰이 큼. Papers with Code(현재 Hugging Face의 Trending Papers로 대체)도 좋은 출발점.
- 소셜: X(트위터)에 활발한 커뮤니티·팔로우할 인물 다수.
- 영상: YouTube — Yannic Kilcher(2017년 트랜스포머 논문을 처음 상세히 다룬 유튜버), Andrej Karpathy(최고의 교육자 중 하나). 그리고 회사 블로그·이 수업의 스터디 가이드(매년 갱신 예정, 여러 언어로 제공).

**맺음말**: 두 강사(Afshin·Shervin)는 금요일 저녁에 와준 수강생들과 온라인 시청자에게 감사를 전하며, 좋은 질문들이 수업을 훌륭하게 만들었다고 했습니다. 그리고 마지막으로 '곰인형'을 다시 등장시키며 모두의 행운을 빌었습니다.

**용어 설명**
- arXiv / NeurIPS / Papers with Code / Hugging Face Trending Papers: 최신 연구를 따라가는 자료.
- Yannic Kilcher / Andrej Karpathy: 논문·개념 해설로 유명한 인물.

---

## 4. 핵심 용어집

| 영문 | 한국어 | 뜻 |
|---|---|---|
| DeepSeek-OCR / 2D RoPE | — | 비전 토큰 표현력 / 2D 위치 |
| Muon / MuonClip | 뮤온 | Adam에 도전하는 새 최적화기(Kimi K2) |
| RMSNorm / GELU | — | 최근 정규화 / 활성함수 |
| model collapse | 모델 붕괴 | LLM 생성 데이터 학습의 분포 손상 |
| mid-training | 중간학습 | 사전학습 후 고품질 단계 |
| SLM | 소형 언어모델 | 비용 효율 소형 모델 |
| continual learning | 지속 학습 | 배포 후 계속 학습(열린 과제) |
| hallucination | 환각 | 사실과 다른 생성(설계상 본질) |

---

## 5. 복습 문제

1. 모달리티 간 차용의 예 두 가지(예: 디퓨전 트랜스포머, DeepSeek-OCR, 2D RoPE)를 설명하라.
2. 트랜스포머에서 아직 연구 중인 설계 요소 세 가지는?
3. 현재 LLM 아키텍처의 근본 과제(지속 학습·환각 등)는 무엇인가?

---

## 부록. 실무 적용 포인트 (NLP·LLM 기술 내용 한정)

- **"설계가 아직 확정이 아니다"는 점은 "AI는 빠르게 변한다"는 실무 전제다.** 최적화기·정규화·구조가 계속 바뀌므로, 특정 모델·도구에 고착하기보다 동향을 주기적으로 점검하는 자세가 필요하다. 스터디 가이드·arXiv·해설 영상 등이 따라가는 통로다.
- **모델 붕괴·데이터 큐레이션은 "AI 시대 데이터 품질의 중요성"을 일깨운다.** 인터넷이 AI 생성물로 채워질수록 고품질·사람 검증 데이터의 가치가 커진다. 기관 고유의 정제된 데이터(검증된 내부 자료)가 점점 더 귀한 자산이 된다는 의미다.
- **환각·지속 학습·고객센터 사례는 "AI의 한계를 알고 쓰라"는 결론이다.** 환각은 설계상 본질이고, 모델은 배포 후 스스로 갱신되지 않으며, 사람의 분별을 완전히 대체하긴 어렵다. AI를 보조로 두고 사람이 검증·판단하는 구조가 — 특히 대민·사실성 업무에서 — 여전히 옳다.

---

## ✅ Lecture 9 변환 완료 요약

**산출물**: `Stanford CME295_Lecture 9/` 폴더에 섹션 1~6

### 섹션 구성
| 섹션 | 구간 | 내용 |
|---|---|---|
| 1 | 00:05~15:26 | 전체 복습① — 트랜스포머~LLM 디코딩(Lec 1~3) |
| 2 | 15:26~38:22 | 전체 복습② — 학습·정렬·추론(Lec 4~6) |
| 3 | 38:22~48:55 | 전체 복습③ — RAG·도구·평가(Lec 7~8) + 시험 범위 |
| 4 | 48:55~1:03:48 | 트렌드① — Vision Transformer·멀티모달(VLM) |
| 5 | 1:03:48~1:23:21 | 트렌드② — 디퓨전 기반 LLM(MDM) |
| 6 | 1:23:53~끝 | 마무리 — 모달리티 교차·연구 동향·미래·자료 |

### 자막 오인식 보정 (Lecture 9)
- `birds` → **BERT**, `rope` → **RoPE**, `PO/PPU` → **PPO**, `Bradley Terry` → **Bradley-Terry**
- `HPM` → **HBM**, `SRAMM` → **SRAM**, `AIM` → **AIME**, `depo` → **DAPO**
- `VIT` → **ViT**, `lava` → **LLaVA**, `llama 3` → **Llama 3**
- `LADA` → **LLaDA**, `MDM/DLLM` → **MDM/dLLM**, `ARM` → **ARM**
- `deepsek OCR` → **DeepSeek-OCR**, `Kim K2` → **Kimi K2**, `muon` → **Muon/MuonClip**
- `atom` → **Adam**, `gausian error linear units` → **GELU**, `kirhoff` → **Kirchhoff**
- `chat GPT's atlas` → **ChatGPT Atlas**, `Yanik Kilchshire` → **Yannic Kilcher**, `Andrish Karpath` → **Andrej Karpathy**, `new rips` → **NeurIPS**, `paper with code` → **Papers with Code**

### 짚을 점
- LLaDA 정식 약자를 강사도 "전체 약자가 기억 안 난다"고 함 → 본문에 "Large Language Diffusion with mAsking"로 보강.
- 복습 섹션(1~3)은 이미 다룬 내용이라 다소 압축하되 한 줄기로 정리. 새 내용(트렌드·마무리)은 충실히 작성.

---

> Lecture 9 변환 완료. 이로써 Lecture 4~9 전체 변환이 끝났습니다.
